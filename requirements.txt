# Core dependencies for Adaptive Database Framework
pymysql==1.1.0
cryptography>=41.0.0
pymongo==4.6.0
python-dotenv==1.0.0

# HTTP client (optional - only needed for data stream)
requests==2.31.0

# Dashboard server
fastapi>=0.110.0
uvicorn[standard]>=0.27.0

# Note: pydantic and httpx removed since they're not actually used in the core code
